import Layout from "../components/Layout"
import { Typography } from "@mui/material"

export default function EmployeeList() {
  return (
    <Layout>
      <Typography variant="h1" component="h1">
        This is the employee list page
      </Typography>
    </Layout>
  )
}

